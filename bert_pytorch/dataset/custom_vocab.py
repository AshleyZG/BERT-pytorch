class CustomVocab(object):
    """docstring for CustomVocab"""

    def __init__(self, vocab_path, data_path):
        super(CustomVocab, self).__init__()
        # self.arg = arg
        self.vocab_path = vocab_path
        # with open(vocab_path,'r')
